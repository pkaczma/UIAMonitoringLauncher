<UserControl x:Class="MonitoringLauncherWPF.Views.AutomationItemControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Cursor="Arrow">
    
    <Expander x:Name="MainExpander" Style="{StaticResource MacroCard}">
        <Expander.Header>
            <Grid Margin="0,0,10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/> 
                    <ColumnDefinition Width="Auto"/> 
                    <ColumnDefinition Width="*"/>    
                    <ColumnDefinition Width="120"/>  
                    <ColumnDefinition Width="140"/>  
                </Grid.ColumnDefinitions>
                
                <!-- CheckBox z użyciem nowego stylu -->
                <CheckBox x:Name="ItemCheckBox" 
                          Style="{StaticResource ModernCheckBox}"
                          VerticalAlignment="Center" Margin="0,0,15,0" 
                          Checked="ItemCheckBox_CheckedChanged" 
                          Unchecked="ItemCheckBox_CheckedChanged"/>
                
                <Border x:Name="IconBorder" Grid.Column="1" Width="40" Height="40" CornerRadius="8" Background="#1AFFFFFF" Margin="0,0,15,0" VerticalAlignment="Center">
                    <TextBlock x:Name="IconText" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                
                <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,0,20,0">
                    <TextBlock x:Name="TitleText" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" TextWrapping="Wrap"/>
                    <TextBlock x:Name="DescText" FontSize="13" Foreground="{StaticResource TextSecondary}" Margin="0,3,0,0" TextWrapping="Wrap"/>
                </StackPanel>
                
                <Border x:Name="StatusBadge" Grid.Column="3" Style="{StaticResource StatusBadge}" Margin="0,0,20,0">
                    <TextBlock x:Name="StatusText" FontSize="12" FontWeight="Bold"/>
                </Border>
                
                <Button x:Name="ActionButton" Grid.Column="4" HorizontalAlignment="Stretch"/>
            </Grid>
        </Expander.Header>
        
        <StackPanel>
            <Border BorderBrush="{StaticResource BorderSubtle}" BorderThickness="0,1,0,0" Margin="0,0,0,15" Padding="0,15,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="EditButton" Content="Edit Configuration" Style="{StaticResource GhostButton}" Margin="0,0,10,0"/>
                    <Button x:Name="DeleteButton" Content="Delete" Style="{StaticResource DangerButton}"/>
                </StackPanel>
            </Border>
            <Border x:Name="LogBorder" CornerRadius="6" BorderThickness="1" Padding="12">
                <TextBox x:Name="LogTextBox" IsReadOnly="True" Background="Transparent" BorderThickness="0" FontFamily="Consolas" FontSize="13" Height="Auto" VerticalScrollBarVisibility="Auto" Cursor="IBeam"/>
            </Border>
        </StackPanel>
    </Expander>
</UserControl>